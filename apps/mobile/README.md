# Mobile App

This folder will hold the React Native + Expo app for SmartAqua.

Planned shape:

- shared Firebase backend
- shared auth and role logic
- mobile-friendly screens that match the web design system

Local env:

- put Expo Firebase values in `apps/mobile/.env`
- use the `EXPO_PUBLIC_FIREBASE_*` names
